namespace Dev.Enum {

    /**
     * listeners actions
     */
    export enum DataListener {
        keydown = "keydown",
        message = "message"
    };

    export enum KeyCode {
        up = "KeyW",
        left = "KeyA",
        down = "KeyS",
        right = "KeyD",
        fire = "Space"
    }
}